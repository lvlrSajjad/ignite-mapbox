import * as React from 'react'
import {Component} from 'react';
import {View} from "react-native";
import MapboxGL from '@mapbox/react-native-mapbox-gl';
import MapboxConfig from "../../config/MapboxConfig";

MapboxGL.setAccessToken(MapboxConfig.accessToken);

interface <%= props.name %>ComponentProps {
  isDarkMode?:boolean
}

export default class Component extends Component<<%= props.name %>ComponentProps> {
  render() {

    return (
      <View style={{flex:1}}>
          <MapboxGL.MapView
            styleURL={this.props.isDarkMode?'mapbox://styles/mapbox/dark-v9':'mapbox://styles/mapbox/light-v9'}
            zoomLevel={15}
            centerCoordinate={[51.3890,35.6892]}
            style={{flex:1}}
           >
          </MapboxGL.MapView>
      </View>
    );
  }
}
